# --- Obsidian State (Ignore) ---
.obsidian/workspace.json
.obsidian/graph.json
.obsidian/core-plugins-migration.json

# --- Python bytecode (MRP Pipeline generated) ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- MRP Pipeline checkpoints (auto-generated on crash, safe to delete) ---
05_journal/mrp_checkpoint_*.json

# --- Temporary Files ---


# --- Plugin Code (Optional) ---
# Nếu bạn muốn lưu toàn bộ plugin (bao gồm cả code plugin bạn tự viết) 
# thì ĐỪNG bỏ comment 2 dòng dưới đây. 
# Nếu bạn chỉ muốn lưu cài đặt (data.json) mà không muốn lưu code plugin tải về, 
# việc cấu hình sẽ phức tạp hơn. Lời khuyên cho dev là cứ lưu hết.

# (Không ignore plugins/ để giữ lại code plugin daily-nav của bạn)